xpas
==== 
Assembler for the XPVM.

The assembler currently parses xpvm assembler formatted programs and can encode
a few instructions. Enough to generate a rudimentary xpvm object file with the
VM can run.
